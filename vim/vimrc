" Vundle bootstrap settings
set nocompatible

filetype off
filetype plugin indent off
set rtp+=~/.vim/bundle/vundle/
call vundle#rc()

Bundle 'gmarik/vundle'

" My Bundles
Bundle 'wgibbs/vim-irblack'
Bundle 'kml/vim-irblack-plus'
Bundle 's-urbaniak/xoria256'
Bundle 'godlygeek/tabular'
Bundle 'Rip-Rip/clang_complete'
Bundle 'scrooloose/nerdtree'

colorscheme xoria256

" C
autocmd filetype c noremap <buffer> <LocalLeader>l :%!indent -kr -ts4 --no-tabs<cr>
autocmd filetype cpp noremap <buffer> <LocalLeader>l :%!indent -kr -ts4 --no-tabs<cr>

" Python
autocmd filetype python set listchars=tab:>.,trail:.,extends:#,nbsp:.

" XML
autocmd filetype xml map <LocalLeader>l :%!xmllint --format -<CR>

" C
autocmd filetype c map <LocalLeader>l :%!indent --no-tabs --indent-level 4 -kr<CR>
autocmd filetype cpp map <LocalLeader>l :%!indent --no-tabs --indent-level 4 -kr<CR>

" Google Go
filetype off
set runtimepath+=$GODIST/misc/vim

autocmd filetype go map K <Plug>(godoc-keyword)
autocmd filetype go map <LocalLeader>l :Fmt<CR>
autocmd filetype go set noexpandtab
autocmd filetype go set makeprg=go\ install

" Global settings

" configure nerdtree to change directories
let g:NERDTreeChDirMode=2

" enable indentation for filetype plugins
filetype on
filetype plugin indent on

" enable ruler and always turn on status bar
set ruler
set ls=2

" show relative numbers in side bar
set rnu

" enable syntax highlighting
syntax on

" enable ignore case and smartcase
set ic
set scs

" enable incremental search
set is

" show command in last line of the screen
set showcmd

" set up indentation using '4 whitespace chars' style
set ts=4
set shiftwidth=4
set softtabstop=4
set expandtab

nmap ; :
