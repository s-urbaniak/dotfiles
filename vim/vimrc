colorscheme delek

if executable("ag")
    set grepprg=ag\ --nogroup\ --nocolor
    let g:ctrlp_user_command = 'ag %s -l --nocolor -g ""'
endif

" XML
autocmd filetype xml map <LocalLeader>l :%!xmllint --format -<CR>

" use real tabs instead of spaces for go
autocmd filetype go set noexpandtab
autocmd filetype go map <LocalLeader>i :%!goimports "%" 2>goimports.log<CR>

" enable indentation for filetype plugins
filetype on
filetype plugin indent on

" enable ruler and always turn on status bar
set ruler
set ls=2

" enable syntax highlighting
syntax on

" enable ignore case and smartcase
set ic
set scs

" enable incremental search
set is

" show command in last line of the screen
set showcmd

" set up indentation using '4 whitespace chars' style
set ts=4
set shiftwidth=4
set softtabstop=4
set expandtab

" enable mouse in console
set mouse=a
set autoread

" enable visual bell
set vb

" disable entering ex mode
nnoremap Q <nop>

" Avoiding the "Hit ENTER to continue" prompts
set shortmess=a
